[project]
name = "microseg"
version = "0.1.0"
authors = [{name="Martin Vogt"}]

dependencies = [
    "networkx>=3.6",
    "matplotlib>=3.10.0",
    "pyvis>=0.3.1"
]

[tool.setuptools.packages.find]
where = ["src"]